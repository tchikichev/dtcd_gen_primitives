
// take from electrical primitives ext
const nominalColors = [];

// function addPropsbyprimitiveName(liveDash, nodeParamsMapping, graphObjectTypes) {
//     const defExpr = {
//       "type": "expression",
//       "expression": "",
//       "status": "completed",
//       "value": ""
//     }
  
//     Object.entries(graphObjectTypes).forEach(([target_primitiveName, node_class]) => {
//       let targetNodes = liveDash.masterGraph.nodes.toArray().filter(
//         node => node.tag.primitiveName === target_primitiveName
//       );
//       let addProps = nodeParamsMapping[node_class];
  
//       targetNodes.forEach(node => {
//         Object.entries(addProps).map(([p, info]) => {
//           node.tag.properties[p] = defExpr;
//         })
//       })
  
//     })
//   };
  
